name: Deploy .NET Core App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: sensoriz-monitoring
          path: SensorizMonitoring/bin/Release/net8.0/linux-x64/

      - name: Download artifact
        uses: actions/download-artifact@v3
        with:
          name: sensoriz-monitoring
          path: SensorizMonitoring/bin/Release/net8.0/linux-x64/

      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: 146.190.220.28
          username: root
          password: qpwoeiruTY3164@
          script: |
            mkdir -p /home/monitoring
            mkdir -p /home/runner/work/srz.monitoring
            cp -r SensorizMonitoring/bin/Release/net8.0/linux-x64/* /home/runner/work/srz.monitoring/
            ls -l /home/runner/work/srz.monitoring/
            cp -r /home/runner/work/srz.monitoring/* /home/monitoring/
            pm2 restart all
